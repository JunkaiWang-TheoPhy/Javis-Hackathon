# 当前 Repo 体量概览

## 1. 文档目的

这份文档把本次对话里对仓库体量的判断整理成一个可引用的快照，方便后续判断：

- 当前 repo 更接近什么规模
- 体积主要被什么内容拉高
- 哪些内容是核心代码，哪些内容更像实验、镜像或工作区产物

说明：

- 这是 `2026-03-17` 在本地工作区得到的快照
- 数字会随着后续同步、文档和镜像内容变化而变化

## 2. 总体规模快照

- 工作区总磁盘占用：约 `689M`
- `.git` 对象库：约 `54M`
- Git 已跟踪文件数：`1274`
- 工作区总文件数：`10801`
- Git 已跟踪文件总字节数：约 `108,728,775` bytes，约 `103.7M`

## 3. 代码和文本规模

按 `cloc --vcs=git` 统计，当前 Git 跟踪的可识别文本/代码文件为：

- 文件数：`960`
- 总行数：`327,962`
- 其中代码行：`276,576`
- 空行：`15,959`
- 注释行：`35,427`

主要构成是：

- JavaScript：`152,131` 行代码
- JSON：`54,918` 行
- TypeScript：`34,176` 行代码
- Markdown：`20,863` 行
- Python：`8,304` 行代码
- Kotlin：`1,825` 行代码
- Shell：约 `2,246` 行代码（Bourne Shell + Bash）

## 4. 体积最大的目录

当前工作区里最占空间的目录主要是：

- `.worktrees`：约 `305.9M`
- `0313`：约 `136.1M`
- `tmp`：约 `81.3M`
- `.git`：约 `54.2M`
- `OpenClaw`：约 `50.3M`
- `devices`：约 `47.8M`
- `Readme`：约 `12.3M`

这说明当前 repo 的体量不只是由“主代码”决定，还明显受以下内容影响：

- 多 worktree 工作副本
- 原型蓝图和整包镜像
- 临时数据
- 演示材料和导出文档

## 5. 这说明了什么

从工程组织上看，这个仓库不是一个单一、干净的产品仓，而更像一个：

- 原型仓
- 集成仓
- 实验仓
- 文档仓
- 运行态镜像仓

它的价值不只是“代码量”，而是“把 Mira 的 persona、runtime、plugins、skills、硬件桥接、设计思考和 devbox 镜像放在同一个工作区里”。

## 6. 如果只从发布视角看

如果后续要整理为 `Mira_Released_Version`，当前 repo 里的内容大致可以分成三类：

### A. 发布核心内容

- `Mira_v1`
- `0313/openclaw-ha-blueprint`
- `OpenClaw/devbox/project/openclaw-ha-blueprint-memory`
- `devices/mi-band-9-pro`
- 关键 README / docs / specs

### B. 运行态镜像和归档内容

- `OpenClaw/devbox/.openclaw/...`
- `openclaw/devbox/...`
- 部分 sessions、memory、workspace 镜像

### C. 体量放大项，但不一定属于正式发布版

- `.worktrees`
- `tmp`
- 大型导出资料
- 历史材料和中间产物

## 7. 结论

当前 repo 的体量已经超过一个“轻量 demo 仓”，但还没有被整理成正式产品仓。

更准确的判断是：

- 它是一个中等偏大的、多技术栈、多来源的原型集成仓
- 它既有正式可发布的核心材料，也混有较多实验、镜像和工作区放大项

这也是为什么后续要单独整理出 `Mira_Released_Version`：

- 不是因为现有 repo 没内容
- 而是因为它的内容已经很多，但还没有被整理成适合公开发布的结构

# Mira Release Export Package And License Placeholder Plan

## Goal

Add the last split-preparation pieces needed to export `Mira_Released_Version/` as a standalone repository package.

## Steps

1. Add `LICENSE.placeholder.md`.
2. Add a repository split checklist.
3. Add a root export script.
4. Add a test for the export script.
5. Update release docs to point at the export path and placeholder license.
6. Run root verification, root tests, and export the package.
7. Verify the exported directory package as its own repository root.

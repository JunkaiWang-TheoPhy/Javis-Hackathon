# README

| 中文 | English |
| --- | --- |
| 本目录是对 `docs/mira_package_clean/main.tex` 的忠实 Markdown 转换版本。 | This directory is a faithful Markdown conversion of `docs/mira_package_clean/main.tex`. |
| 目标是保留原 LaTeX 文档的章节顺序、正文内容、表格信息、图片位置和关键公式，同时用更适合 Markdown 的结构重排版式。 | The goal is to preserve the section order, body text, table content, image placement, and key formulas from the original LaTeX document while reformatting the layout into Markdown-friendly structures. |
| 原始 LaTeX 中的 TikZ 框图、色块卡片和时间线，已转换为 Markdown 标题、列表、表格和代码块；内容保持忠实，视觉表达改为 Markdown 可承载的形式。 | TikZ diagrams, colored cards, and timeline layouts from the original LaTeX have been converted into Markdown headings, lists, tables, and code blocks; the content remains faithful while the presentation is adapted to what Markdown can support. |

## Files

| 中文 | English |
| --- | --- |
| `main.md`：项目说明书的 Markdown 主文件。 | `main.md`: the main Markdown version of the project brief. |
| `assets/cover-banner.png`：封面横幅图，从原 LaTeX 资源目录复制。 | `assets/cover-banner.png`: cover banner image copied from the original LaTeX assets directory. |
| `assets/social-value-table.png`：社会价值表格图，从原 LaTeX 资源目录复制。 | `assets/social-value-table.png`: social value table image copied from the original LaTeX assets directory. |
| `README.md`：本说明文件，采用中英文并列表达。 | `README.md`: this guide file, written in side-by-side Chinese and English. |

## Source Mapping

| 中文 | English |
| --- | --- |
| 原始来源目录：`docs/mira_package_clean` | Original source directory: `docs/mira_package_clean` |
| 原始 LaTeX 文件：`docs/mira_package_clean/main.tex` | Original LaTeX file: `docs/mira_package_clean/main.tex` |
| 原始编译产物：`docs/mira_package_clean/main.pdf` | Original compiled artifact: `docs/mira_package_clean/main.pdf` |
| 本次转换没有改动原始 LaTeX 目录，只是在新目录中生成 Markdown 版本。 | This conversion does not modify the original LaTeX directory; it only creates a Markdown version in a new folder. |

## Notes

| 中文 | English |
| --- | --- |
| Markdown 不具备 LaTeX 的精确版式能力，因此“忠实”主要体现在内容、结构和语义层面，而不是逐像素复刻。 | Markdown does not support LaTeX-level precise layout, so "faithful" here primarily means fidelity in content, structure, and semantics rather than pixel-perfect reproduction. |
| 若后续需要，我可以继续补一份适合 GitHub/Notion 展示的精简版 Markdown，或再导出一份更适合网页展示的 HTML。 | If needed, a simplified Markdown version optimized for GitHub/Notion or an HTML version for web presentation can be created next. |
